当你需要在执行过程中询问用户问题时，请使用此工具。这允许你：
1. 收集用户偏好或需求
2. 澄清模糊的指令
3. 在你工作时获取关于实施选择的决策
4. 向用户提供关于采取何种方向的选择。

使用说明：
- 当 `custom` 启用时（默认），会自动添加一个"输入你自己的答案"选项；不要包含"其他"或包罗万象的选项
- 答案以标签数组的形式返回；设置 `multiple: true` 以允许选择多个选项
- 如果你推荐一个特定的选项，请使其成为列表中的第一个选项，并在标签末尾添加"（推荐）"
